# Chrome Bilingual Translator

Chrome Manifest V3 extension for on-demand bilingual webpage translation.

## Development

1. Install dependencies with `npm install`
2. Build with `npm run build`
3. Load the project in `chrome://extensions` using `Load unpacked`

## Status

Initial shell in progress.

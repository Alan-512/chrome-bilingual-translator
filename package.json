{
  "name": "chrome-bilingual-translator",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "A Chrome extension for on-demand bilingual webpage translation.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/chrome": "^0.1.24",
    "@types/jsdom": "^21.1.7",
    "esbuild": "^0.25.9",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
